@page
@inherits Acesoft.Web.UI.RazorPages.WebPageBase
@inject Tenant Tenant
@{
    ViewData["Title"] = "loading...";

    // redirect to index page.
    HttpContext.Response.Redirect(Tenant.IndexPage);
}