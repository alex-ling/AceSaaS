@page
@inherits Acesoft.Web.UI.RazorPages.WebPageBase
@{ 
    ViewData["Title"] = "进入";

    var roles = Ace.AC.Roles;
    if (roles.Contains(978665085142564864) || roles.Contains(305049547120312320))
    {
        // 前端角色：IoT用户、Viewer用户
        App.Context.Response.Redirect("/iot/index/front");
    }
    else
    {
        App.Context.Response.Redirect("/plat/index");
    }
}